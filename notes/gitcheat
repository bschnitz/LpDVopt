# Git Cheat Sheet by Bo

# find files, searching in all branches
# Example to find all files containing 'SD'
git log --all --name-only --pretty=format: | sort -u | grep .*SD.*

# graphical tool to inspect commits and branches
gitk

# push new branch: git push  <REMOTENAME> <BRANCHNAME>
git push origin newbranch

# delete a local branch
git branch -D lpdv.stable

# delete a remote branch
git push origin --delete branch_name

# add a file file to the last commit
git commit --amend <filename>

# vim: set ft=sh :
